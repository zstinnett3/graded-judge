# OpenAI (for tier1/tier2 when provider=openai)
OPENAI_API_KEY=sk-...

# AWS (for tier1/tier2 when provider=bedrock)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# Ollama runs locally at http://localhost:11434 - no key needed
